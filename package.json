{
  "name": "regal-walkthrough",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "3D Gaussian Splatting walkthrough for Regal Homes — built on PlayCanvas, deployed to GitHub Pages.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "verify": "vite build && node _verify.cjs"
  },
  "dependencies": {
    "playcanvas": "^2.10.0"
  },
  "devDependencies": {
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
